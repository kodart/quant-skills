{
  "api_version": {
    "min": 18,
    "max": 19
  },
  "note": "The bt-api API_VERSION range this bundle's guidance was written against. One number, not a matrix: bt-mcp refuses to serve unless bt-server reports the exact version it was built against, so whenever the endpoint answers at all, adapter and server agree. Widen `max` when a bump is verified not to change what the skills say; raise `min` when it does. 9->10 widened `max` (2026-08-05): the catalog reshape (#263) changed `GET /v1/catalog/{indicators,detectors}` breakingly, but no skill in this bundle references those routes, the DTO or its `category` field — checked, not assumed — so nothing this bundle SAYS is affected. 10->11 widened `max` (2026-08-05) on the same check, re-run rather than inherited: built-in detectors now report `type: \"episodes\"` where they reported `type: \"detector\"` on `GET /v1/catalog/detectors`, and grepping all five skills for `catalog/detectors`, `catalog/indicators`, `CatalogKindDto`, `CatalogOutputDto`, `episodes` and `detector` returned nothing at all — no skill names either route, the DTO, or any `ty` value, so no sentence in this bundle can go stale on the rename. 11->12 widened `max` (2026-08-09) on the same check, re-run a third time and widened to include `arity`: `GET /v1/catalog/{indicators,detectors}` now reports arity and outputs per version and filters on the union across versions (#390), and grepping all five skills for both route paths, all three catalog DTO names and the bare word `arity` returned nothing — no skill reads a catalog row at all, so nothing this bundle says depends on which version's shape a row summarizes. 12->13 widened `max` (2026-08-11): `CatalogKindDto` gained `params`, recovered from the engine's own chart factories (#511) — purely ADDITIVE, and the charting skill's new sentence about it is written to degrade (\"if the row carries `params`\"), so the bundle stays correct against a server at 12 that omits the field. `min` therefore stays where it is: nothing the bundle says becomes WRONG on an older server, it just has less to read. 13->14 widened `max` (2026-08-11, #526 follow-on): `PerRunSummaryDto` gained `rejection_reasons` — `orders_rejected` broken down by category, a sibling of `metrics` — and this same change lifted it into both bt-mcp read tools, `get_results` and `query_runs`, so the field DOES reach an agent as of this commit. Checked, not assumed: grepped all five skills (and their `references/`) for `rejection_reasons`, `PerRunSummaryDto`, and, broadly, every case-insensitive occurrence of `reject` — `backtest-analyst/SKILL.md` and its `references/backtest-result-contract.md` are the only hits, and both discuss only the bare `orders_rejected` count, saying nothing about a per-reason breakdown. So even though the field is now live on both tools, no skill TEXT describes it, which is the thing this file tracks — nothing this bundle SAYS is affected either direction. `min` stays where it is for the same reason as 12->13: the bundle has no sentence that could go stale by the field's absence — an agent reading this bundle against a server at 13 simply gets less to read (no `rejection_reasons` key on the wire, and no skill sentence promising one). `min` moves only once a skill gains a sentence that assumes `rejection_reasons` is present. 13->14 RAISED `min` (2026-08-12, strategy-coder-sdk phases 2-3, #524/#526-line follow-on): unlike every entry above, this bump changed what a skill's TEXT says, not just what the wire carries, so this is the raise case the note above names rather than another widen. Checked, not assumed — grepped `skills/strategy-coder-sdk/` (SKILL.md and `references/`) and found four load-bearing changes, all landed in this release's commits `1f466103`/`7b31a436`/`aad9f7e4`: (1) `export_module!`'s three-element strategy form, `(\"name\", make, schema)`, and `#[derive(bt_module_sdk::schemars::JsonSchema)]` as the schema source — SKILL.md lines 17-20, 116, 135-178; against a server/bt-mcp predating the schema-canonicalization work this describes a form that either does not exist or is not enforced the way stated. (2) the prose parameter-list instruction is GONE, replaced by \"The schema IS the parameter list\" (SKILL.md:164) — an agent reading the old prose-list guidance against a server that still expects it would be wrong in the other direction. (3) `run_backtest`/`run_sweep`/`run_walk_forward` now teach plain-JSON `config`/`configs` on the module lane as \"the normal path\" (SKILL.md:128, 205, 224, 231, 348-350) — grepped for `config_hex` and confirmed the skill now presents the JSON lane first and the hex lane as the schema-less fallback; against bt-mcp before the JSON-config-lane commit, `check_lane` refused a JSON `config`/`configs` on the module lane outright (per `backtest-engine/CLAUDE.md`'s bt-mcp entry), so an agent following this guidance against that older adapter is refused. (4) `Cmd::target`'s unit is now stated explicitly — \"desired signed position, in lots\", absolute not delta, idempotent — plus a worked equity-to-lots conversion example (SKILL.md:298-320); this is new factual content, not a rephrase, so its absence/wrongness against an older bundle-server pairing is a real gap being closed, not evidence for a bump by itself. `references/sdk-surface.md` (1220 lines, generated from engine source: re-export paths, stream `PayloadKind`s, every `Cmd` variant with its unit, and the node catalogue including `BarAggregator`/`TimerBarAggregator` and the indicator/detector families) is entirely new and reinforces (1) and (4) — confirmed present via grep for `BarAggregator`, `PayloadKind`, `Cmd::Target`. One claim in this task's own brief did NOT check out: it asserted `EngineSpecDto.account` (`\"cash\"`/`\"margin\"`) is 'now described in the submit tools'. Grepped all five skills and their `references/` for `AccountSpec`, the literal strings `\"cash\"`/`\"margin\"`, and `EngineSpecDto`/`EngineSpec`/`engine_spec` case-insensitively — the only hits are `references/sdk-surface.md`'s auto-generated type-alias rows for unrelated `bt_model::account::{Account, AccountBalance, AccountKind, AccountId}` structs (SDK re-export paths, not submit-tool guidance) and one line in `charting/SKILL.md` (\"the same EngineSpec shape run_backtest takes\", no field-level detail). No skill sentence promises or depends on an `account` field; per `bt-api/src/lib.rs`'s own `no bump (2026-08-11, account on the engine spec, #524)` entry, that field never shipped without `rejection_reasons` in any deployed build, so there is no older-server pairing where the bundle's silence on it could go stale either. It does not weaken the raise — (1)-(4) already establish it — but it is not evidence for it, and is recorded here rather than silently dropped so the next reader does not re-derive it. `min` therefore moves from 13 to 14: a server at 13 predates the commits that landed the JSON config lane and the current `export_module!`/schema guidance in bt-mcp, and following this bundle's SKILL.md against it produces refused submissions, not just missing fields. 14->14 no range change (2026-08-13, sr_levels + trendlines): verified against `bt-api/src/lib.rs` that API_VERSION stayed at 14 for this change (recorded there as a \"no bump\" entry, two additive catalog rows) — the charting and market-screener skills now name `sr_levels`/`trendlines`, which are catalog ROWS, not shapes, and the skill text checks `list_detectors` before relying on them, so the bundle stays correct against a server at 14 that predates the rows. 14->15 widened `max` (2026-08-13, scan lane): API_VERSION 15 adds POST /v1/scan and the scan_datasets tool; the market-screener skill's scan section is written to degrade (\"if your tool list includes scan_datasets\"), and no other skill sentence assumes the route exists, so the bundle stays correct against a server at 14 that lacks it. min stays 14: nothing the bundle says becomes wrong on 14 — the scan section explicitly handles the tool's absence. This entry supersedes the interim 14->15 note appended earlier in this same change (scan lane task 1 of 6), which was written before the market-screener skill's scan section existed to check against and only cleared the version-range floor; this entry is the check re-run against the finished skill text, and replaces rather than duplicates that earlier note. 15->16 RAISED min and widened max together (2026-08-14, service consolidation): API_VERSION 16 drops check_strategy_source and publish_strategy_source from the tool surface entirely (bt-mcp no longer talks to a build service at all), and this same change deleted the strategy-coder-sdk and indicator-coder-sdk skills from the bundle — the two skills whose whole purpose was authoring guidance for those tools. There is therefore no server in this range an agent can usefully run this bundle against except 16 itself: a server at 14 or 15 still serves the removed tools, but the skills that taught an agent to use them are gone from the bundle, and a server at 16 refuses them outright. min moves to 16 for the same reason a doc-accuracy edit would not have justified moving it — this is a REMOVAL of what the bundle can do, not an additive change an older server merely lacks. 16->16 no range change (2026-08-14, task 11 skills sweep): API_VERSION stays 16; this entry records what the BUNDLE says changing, not the wire. The `charting` skill was retargeted rather than deleted — its engine chart lane (`render_chart`, `render_chart_app`, `chart_widget_payload`, `chart_status`) was already written against the surviving surface and survives nearly verbatim, so what went was the dead half: the two-runtime routing table, the `quant.create_market_chart` workflow, and the `module@version::component` authoring lane. Five reference files went with it — `allowed-chart-scripts.md` (fourteen `scripts/quant-chart` CLI invocations, a CLI deleted in task 1), `inline-chat-charts.md` (taught `quant.get_widget_payload`, which the surviving lane explicitly PROHIBITS calling directly), `market-chart-defaults.md` (Yahoo-style `BTC-USD` tickers and `1h` intervals, neither of which `render_chart` takes), `chart-data-context-policy.md` and `chart-artifacts.md` (one surviving idea each, both already stated in SKILL.md). `series-display-rules.md` was rewritten down to its medium-independent panel/overlay policy. `strategy-researcher` lost `quant.web_research`, `quant.x_research`, `quant.analyze_video` and `quant.get_task_status`: those were agent-service tools, never on the MCP surface, and the skill now directs research to the HOST's own search. Checked, not assumed — grepped all five skills for `publish_strategy_source`, `check_strategy_source`, `strategy-coder`, `bt-module-sdk`, `bt-build`, `/v1/bt/`, `chart-data`, `agent-service`, `quant-chart` and `tenant`: `backtest-analyst`, `market-screener` and `risk-reviewer` had ZERO hits and are unchanged. min/max both stay 16: no sentence in this bundle now depends on a version other than 16, and 16 is already the floor set when the authoring tools were removed. 16->17 RAISED min and max together (2026-08-15, the sweep verdict's sample size): a raise rather than a widen because it changes the MEANING of a field the bundle's skills already describe, not just what the wire carries. `OverfittingDto.deflated_sharpe` on the sweep lane is now computed at an EFFECTIVE sample size rather than the raw equity-tick count; on a quote-driven dataset a tick is a quote, and PSR's sqrt(n-1) over observations that are not independent biased the band optimistic. Same field, same [0,1] range, different number where it applies. Scope stated precisely because an earlier draft of this entry overclaimed it: the correction fires only on POSITIVE lag-1 autocorrelation, so a mostly-flat strategy keeps the raw count and its verdict is bit-identical to 16; where it fires the shrink is sqrt((1-rho)/(1+rho)), and it can never RAISE a band. Checked, not assumed: grepped all five skills for `deflated_sharpe`, `overfitting`, `n_trials`, `trial_sharpe_variance` and `verdict` — `backtest-analyst/SKILL.md` and its `references/backtest-result-contract.md` are the hits, and both are updated in this same change to state the effective-sample-size basis and to stop recommending 'run it over more data' as a way to strengthen a verdict. `risk-reviewer` names the verdict without describing how it is computed and needed no edit. min moves because an agent reading this bundle against a 16 server would report the band this bundle's text says `supported` means, computed the old way. NOTE: an earlier revision of this entry also claimed a walk-forward `overfitting` block. That was REVERTED before merge — the estimator benchmarked cold out-of-sample performance against E[max SR], which is the in-sample best-of-N level, so it double-counted selection bias; and every walk-forward fixture uses the `net_pnl` objective, so the code never executed under test. The walk-forward summary carries no `overfitting` key at 17, exactly as at 16. 17->17 no range change (2026-08-16, `warnings` and `rejection_reasons` finally described): API_VERSION stays 17 and neither field is new on the wire — this entry records the BUNDLE gaining sentences about them, which is what this file tracks. `rejection_reasons` has been live since 14, and the 13->14 entry above says in terms that no skill TEXT described it; `warnings` is a bt-mcp construct (`tools/backtest.rs`) that predates 17 as well. Both were found undocumented while driving the whole surface end to end against a running server on 2026-08-16 (scripts/mcp-e2e/verify.py, engine at 3776884d) — so unlike most entries here this one is checked against the WIRE, not only against source. What the run showed: a sweep whose `engine.initial_cash` was too small returned eleven `warnings` sentences, one job-wide and ten per-row, over runs reporting 295,928 rejections against 1,977 fills with `rejection_reasons` `{\"insufficient USDT\": 294479, \"insufficient BTC\": 1449}` — and `backtest-analyst` told an agent to read `orders_rejected` first while saying nothing about either the breakdown or the sentences the server had already written. `backtest-analyst/SKILL.md` gains checklist step 3 (`warnings`) and an amended step 2 (`rejection_reasons`, and the under-funded `initial_cash` it usually means), and `references/backtest-result-contract.md` gains a section stating the exact contract: threshold is strictly more `orders_rejected` than `trade_count` (`REJECTION_DOMINANCE_RATIO = 1.0`), per-row warnings see only the ten `LEADERBOARD_LEN` rows while the job-wide one sees every scored run (which is why it exists — a refused run scores near-zero activity and ranks OFF the leaderboard), the field is ABSENT rather than empty on a healthy job (`skip_serializing_if = \"Vec::is_empty\"`), and a zero-fill run gets its own sentence. Checked, not assumed, for the negative half too: `warnings` is built only in `get_results`' payload, and grepping `bt-mcp/src/tools/research.rs` and `walkforward.rs` confirms `query_runs` carries `rejection_reasons` but no `warnings`, and `get_walk_forward_results` carries neither — it reports `cold_oos.total_oos_orders_rejected` instead. Both new passages say so, so the bundle cannot send an agent looking for a field that tool does not return. min/max both stay 17: nothing here depends on a version other than 17, and a bundle that merely describes fields a server already sends cannot go stale by their absence within a single-version range. 17->17 no range change (2026-08-16, bundle 0.9.1 — the ruin warning reaches the reference): API_VERSION stays 17 and no wire shape moved; this records the BUNDLE text changing, which is what this file tracks. 0.9.0 was published mid-cutover from a tree that predated the `total_return <= -1.0` warning, so its `backtest-analyst/references/backtest-result-contract.md` still asserts \"Two things put sentences in it, and both fire on the same threshold\" — now false in a way an agent acts on: there are three producers and ruin fires on a DIFFERENT threshold, with `orders_rejected` typically 0, so none of the rejection wording covers it. A reader of 0.9.0 against a 17 server meets a warning the bundle does not explain. Version bumped rather than 0.9.0 republished, because 0.9.0 is already public and silently changing a published version's content is how a bundle becomes unauditable. 17->18 RAISED min AND max together (2026-08-17, the chart tools swap names): the MCP Apps view is now `render_chart` and the data-returning lane is `render_chart_fallback`. The surface stays 21 tools — a pure rename — and this is still a raise rather than a widen, for a sharper reason than the count. `render_chart` KEEPS ITS NAME AND CHANGES ITS MEANING: before 18 it returned the drawn series, at 18 it opens a view and returns a bare acknowledgement with no data in it. So an agent on a 17 bundle pointed at an 18 server does NOT get `unknown tool` — the loud failure a rename normally produces, and the reason renames are usually cheap. It gets a SUCCESS carrying nothing it asked for, and goes on to describe a chart whose numbers it never received. The reverse pairing is just as bad: an 18 bundle against a 17 server calls `render_chart` expecting a view and is handed the full series inline, which is exactly the transcript cost the MCP Apps lane exists to avoid. Both directions are silently wrong, which is the case `min` exists for. Checked, not assumed — and the first draft of this entry got it WRONG, which is why the grep is recorded rather than the conclusion: it claimed `charting/SKILL.md` was the only file naming either tool. Grepping all five skills AND their `references/` for `render_chart` returns THREE files. `charting/SKILL.md` (frontmatter, the routing table, the host-capability paragraph, the empty-panel advice, the spec-building steps); `charting/references/series-display-rules.md` (its opening line applies the presentation rules to \"any … spec\", so it names both, and its follow-up-request rule said \"re-call `render_chart`\" — now reworded to \"re-call whichever chart tool you used\", since these rules are deliberately medium-independent and should not prescribe a lane at all); and `market-screener/SKILL.md`, whose screening fetch mechanism is the DATA tool and therefore becomes `render_chart_fallback` — had that one been missed, the screener would have told an agent to fetch bars with a tool that returns no bars, and been handed a bare \"view opened\" to compute NATR from. `backtest-analyst`, `risk-reviewer` and `strategy-researcher` have ZERO hits and are unchanged. This release also carries the framing change the rename was asked for: the routing table was a two-row \"pick by what the user wants\", which invited an agent to surface the choice to the user (\"shall I use render_chart instead?\" — a question about our plumbing, not their chart). It is now a default plus exactly two exceptions, both checkable without asking. And the empty-panel advice, which told an agent to quietly fall back, is rewritten: that silent fallback is what let a SERVER-side defect look like a host limitation for a day on 2026-08-16 — the endpoint was refusing the view's own `resources/read` at admission (`Mcp-Name` read as tools/call-only, against SEP-2243's table; fixed in quant #559) and every session appeared to recover. An empty panel on a host that DID negotiate MCP Apps is now a symptom to state plainly before offering any fallback. Both tools survive deliberately, recorded here so it is not re-litigated as duplication: `render_chart` refuses on any host that does not negotiate MCP Apps (`NO_APP_HOST`, whose refusal text names the fallback), and Claude Code is such a host — measured on the live endpoint as `client \"claude-code\" \"2.1.229\" ... mcp_apps=false extensions=[]` beside `client \"Anthropic/ClaudeAI\" \"1.0.0\" ... mcp_apps=true extensions=[io.modelcontextprotocol/ui]` — so dropping the fallback would leave Claude Code with no charting at all; and the view never returns the series, so an agent that needs the VALUES has no path through it. 18->18 no range change (2026-08-18, bundle 0.10.1 — feed pattern detectors BARS): API_VERSION stays 18 and no wire shape moved; this records the BUNDLE gaining sentences, which is what this file tracks. `pivot_k` counts input SAMPLES, and a declaration bound to `venue.mid` gets one sample per QUOTE, so a candle-sized pivot_k over ticks hunts swings a few quotes wide. MEASURED on production (bt-server 62b4330a, BTCUSDT.BINANCE binance-derived quotes, 14 days): `hns` at `pivot_k: 3` on `venue.mid` returned annotations.count 0, while the same detector at the same pivot_k fed `time_bars(3600000000000) -> bar_close` returned a CONFIRMED episode with full geometry. Neither skill said to chain through bars, and the `bar_close` hop is the one that gets missed — a detector takes f64, so wiring one straight to `time_bars` is refused with `TypeMismatch {expected: \"f64\", got: \"bar\"}`. Checked against engine source, not assumed: every chart-pattern and level kind (`hns`, `double`, `triple`, `triangle`, `wedge`, `flag`, `cup`, `breakout`, `sr_levels`, `trendlines`) resolves `f64_input` in `bt-core/src/chart/registry.rs` and therefore needs the hop, while the eight CANDLE kinds (`hammer`, `doji`, `engulfing`, `star`, `harami`, `piercing`, `soldiers_crows`, `tweezer`) resolve `bar_input` and take the `time_bars` output directly — the same split `bt-server/src/scan.rs`'s `BAR_INPUT_DETECTOR_KINDS` makes. Two skills changed: `charting/SKILL.md` gains the wiring rule in its declaration section, and `market-screener/SKILL.md` gains a pointer to it because its sr_levels/trendlines param tables quote `pivot_k` in CANDLES and are actively misleading without it. Both passages also state that an empty `annotations.count` from a tick-fed declaration is NOT evidence a detector lacks geometry — that inference was drawn and reported as a server limitation twice on 2026-08-17/18, once with the additional false reason that the scan lane runs hns on bars while the chart lane runs it on closes (it does not; hns is not in BAR_INPUT_DETECTOR_KINDS). `min`/`max` both stay 18: the guidance describes behaviour the engine already had, so no sentence here goes stale by an older server's absence of anything. 18->18 no range change (2026-08-18, bundle 0.10.2 — annotations vs markers): API_VERSION stays 18; this records the BUNDLE gaining a sentence. A live Claude Desktop session called `render_chart` correctly, then reported \"the pattern outline still can't be drawn — a hard limit of the engine\" on the evidence that `markers.count` comes back 0. It does, and always will: `markers`, `trades`, `positions` and `equity` are RUN-DETAIL ONLY (`bt-core/src/chart/bundle.rs`, each annotated `/// Run-detail only.`), so on a `{dataset: ...}` source they are unconditionally `{count: 0}` and say nothing about detectors. Detected patterns live in `annotations[].geometry` — labelled points, the neckline polyline, breakout/target/stop levels — which the digest passes through since bundle 0.10.1's engine counterpart (#567). The skill described the four run-detail keys and the `max_annotations_per_decl` budget knob but never said WHERE a detection's geometry appears, so a reader checking the wrong field found a permanently-zero count and concluded the engine could not draw. Third distinct cause of the same user-visible symptom, after the engine genuinely dropping geometry (#567) and detectors being fed ticks instead of bars (#568). 18->19 widened max (2026-08-18, render_chart reports what it drew): API_VERSION 19 adds fields to render_chart's RESULT, not to a request shape — a 200 render now carries range, declarations ({id, kind}) and annotations ({count, by_decl}) alongside the existing view: \"opened\" acknowledgement; a queued render marks summary: \"pending\" with chart_id; a refused spec marks summary: \"refused\" with status/body; a transport failure marks summary: \"unavailable\". This is the opposite shape from 17->18's raise: that bump changed the MEANING of a name a bundle already called, so an old bundle got a silently wrong answer. This one only APPENDS keys a caller never looked for — a bundle written against 18 still parses a 19 server's result correctly, it just never narrates the new tally. charting/SKILL.md's new sentence documenting the field is written to degrade (\"if its result carries an annotations object…\"), which is what makes widening safe rather than merely convenient, and it also states the pending/refused distinction explicitly — an agent that waits on a refusal the way it waits on a pending render is the failure this change exists to prevent. Checked, not assumed: grepped all five skills and their references/ for summary, annotations, by_decl, pending and refused; the only hits outside charting/SKILL.md's new sentence and its pre-existing detector-geometry paragraph are backtest-analyst's unrelated get_results summary object and market-screener's unrelated \"refused at the schema/budget stage\" wording — no other skill's text assumes render_chart's result carries these fields unconditionally. min stays at 18: nothing this bundle says becomes wrong on an 18 server, it just has less to read. 19->19 no range change (2026-08-18, bundle 0.10.4 — review followups for #570): API_VERSION stays 19; this records the BUNDLE and adapter text catching up to what #570 actually shipped, found by a multi-agent review before the bundle publish it gates. charting/SKILL.md lines 82-88 still taught the PRE-#570 contract as absolute (\"a short acknowledgement, not the data—deliberately\", \"You cannot describe what it drew. You never received the series.\") in the very section that introduces the tool, directly contradicting line 181's \"render_chart tells you what it drew\" a hundred lines later — rewritten to state the real contract (range, resolved declarations and an annotation tally, never the series) and point at the fuller paragraph instead of duplicating it, so the two cannot drift apart again. Three doc comments (bt-mcp's chart_app.rs, scopes.rs, and backtest-engine/CLAUDE.md's historical paragraph) still asserted render_chart submits nothing, true before #570 and false after — corrected in place; scopes.rs's Compute classification was already right and is unchanged, only its stated rationale was stale. bt-mcp's chart_summary gained one bool, annotations.truncated, carrying ChartBundle::annotations_truncated through: the summary's per-declaration counts are taken AFTER episodes.rs's default 64-per-declaration cap, so without this a capped count was indistinguishable from a true tally — the exact ambiguity render_chart's summary exists to remove, on the DEFAULT path the skill now recommends. Bundle-level, not per-declaration: it says some declaration was capped somewhere, not which one. tools_chart_app.rs's happy-path test previously asserted only \"opened\" and no series — both true verbatim before #570, since the new code reuses the old acknowledgement object as its base — so it proved nothing about the summary; it now also asserts annotations.count is present and that the mock recorded a POST /v1/charts call. min/max both stay 18/19: nothing here adds or changes a request shape, and a bundle at 0.10.3 already parses a server carrying annotations.truncated correctly — it just has one fewer sentence to narrate it, and the doc/test fixes touch nothing any server sends."
}
